Critical Question for your Troubleshooting Log:
If your US VPC is 10.1.0.0/16 and your Ireland VPC was also 10.1.0.0/16, would the peering connection work? (Think about routing conflicts).

To answer that critical question first: No, the peering would fail if the CIDRs overlapped. If both VPCs were 10.1.0.0/16, the router wouldn't know if a packet for 10.1.0.5 should stay in the US or go to Ireland. Keeping CIDRs unique (like 10.1 and 10.2) is a fundamental rule of network engineering.


Step 4: The Peering Connection & Acceptance
In a cross-region scenario, one side "Requests" and the other side "Accepts." Even though you own both, Terraform needs to talk to both regional APIs.

Add this to your main.tf:

Terraform
# 3. THE PEERING REQUEST (From US to Ireland)
resource "aws_vpc_peering_connection" "us_to_ireland" {
  vpc_id        = aws_vpc.us_vpc.id
  peer_vpc_id   = aws_vpc.ireland_vpc.id
  peer_region   = "eu-west-1"
  auto_accept   = false # Must be false for cross-region
}

# 4. THE PEERING ACCEPTER (In Ireland)
resource "aws_vpc_peering_connection_accepter" "ireland_accepter" {
  provider                  = aws.ireland
  vpc_peering_connection_id = aws_vpc_peering_connection.us_to_ireland.id
  auto_accept               = true
}
Step 5: The "Specialist" Step (Route Tables)
This is the step that separates the experts from the beginners. Creating the connection isn't enough; you have to tell the VPCs how to find each other. Think of it like a new road—you still need to put up the road signs.

Terraform
# 5. ROUTE: US to Ireland (Go to 10.2.x.x via Peering ID)
resource "aws_route" "us_to_ireland_route" {
  route_table_id            = aws_vpc.us_vpc.main_route_table_id
  destination_cidr_block    = aws_vpc.ireland_vpc.cidr_block
  vpc_peering_connection_id = aws_vpc_peering_connection.us_to_ireland.id
}

# 6. ROUTE: Ireland to US (Go to 10.1.x.x via Peering ID)
resource "aws_route" "ireland_to_us_route" {
  provider                  = aws.ireland
  route_table_id            = aws_vpc.ireland_vpc.main_route_table_id
  destination_cidr_block    = aws_vpc.us_vpc.cidr_block
  vpc_peering_connection_id = aws_vpc_peering_connection.us_to_ireland.id
}
Step 6: Potential Troubleshooting Pitfalls (For your Git log)
As you prepare to run terraform apply, keep these potential "Phase 2" issues in mind:

The "Pending Acceptance" Trap: If you forget the accepter resource, the connection will stay in "Pending" status and traffic will never flow.

Security Group Restrictions: Even if the peering is active, if the Ireland instance's Security Group doesn't allow ICMP (Ping) from the US CIDR (10.1.0.0/16), the test will fail.

Main Route Table vs. Subnet Route Table: In this lab, we are using the main_route_table_id. In production, you might have specific tables for Public or Private subnets that need individual updates.


Next Action for you:
Open your main.tf in the Scenario-06 folder.

Paste the full configuration (Providers, VPCs, Peering, and Routes).

Run terraform init and terraform apply.

Once the apply is done, we will launch one micro-instance in each region to perform the cross-Atlantic "Ping" test. Ready to provision the test servers?


Since your VPCs are now successfully connected (peered) and the "road signs" (routes) are in place, it’s time for the final verification. We need to launch one server in the US and one in Ireland to prove that a private IP in N. Virginia can talk to a private IP in Dublin.

Step 7: Launching the Test Instances
Add this to your main.tf. Note that we are using Security Groups to specifically allow ICMP (Ping) traffic from the other VPC's CIDR block.


Step 8: Verification Commands
Run terraform apply to launch the servers.

Get the Private IPs:

Find the Private IP of the Ireland server (it should start with 10.2.x.x).

Find the Private IP of the US server (it should start with 10.1.x.x).

The Cross-Atlantic Ping:
Log into your US Server (via EC2 Instance Connect in the console) and run:
ping <Ireland_Private_IP>


Phase 2: Troubleshooting table. Here are some "Senior Specialist" questions to answer in your documentation:

Did you have to update the Security Group? (Yes, because by default, VPCs don't allow ICMP traffic from external CIDRs).

What was the Latency? When you run the ping, look at the time=ms. Since it’s going from Virginia to Ireland, expect it to be around 70ms - 90ms. This is a great detail to include in your Git repo!

Why use Private IPs? In an interview, explain that using Private IPs over Peering is more secure and cheaper than using Public IPs over the internet.



Classic Terraform "Scope" error! Because we created a new folder for Scenario 06, Terraform doesn't "see" the data source we used in Scenario 10. Each folder is its own separate world (root module).

Also, as a Technical Specialist, here is a pro-tip: AMI IDs are region-specific. You cannot use a US AMI ID in Ireland. To make this truly professional, we should use two separate data sources—one for each region.

The Fix: Add these Data Blocks to your main.tf
Place these at the very top of your main.tf file in the Scenario-06-Global-VPC-Peering folder.

Terraform
# Data Source for US-East-1 (Virginia)
data "aws_ami" "amazon_linux_us" {
  most_recent = true
  owners      = ["amazon"]
  filter {
    name   = "name"
    values = ["amzn2-ami-hvm-*-x86_64-gp2"]
  }
}

# Data Source for EU-West-1 (Ireland)
data "aws_ami" "amazon_linux_ireland" {
  provider    = aws.ireland  # <--- Critical: Tells Terraform to look in Ireland
  most_recent = true
  owners      = ["amazon"]
  filter {
    name   = "name"
    values = ["amzn2-ami-hvm-*-x86_64-gp2"]
  }
}



Update your Instance Blocks
Now, update the aws_instance blocks at the bottom of your file to use these specific regional data sources:

Terraform
# US Instance
resource "aws_instance" "us_server" {
  ami                    = data.aws_ami.amazon_linux_us.id
  instance_type          = "t3.micro"
  vpc_security_group_ids = [aws_security_group.us_sg.id]
  tags                   = { Name = "US-Server-10.1" }
}

# Ireland Instance
resource "aws_instance" "ireland_server" {
  provider               = aws.ireland
  ami                    = data.aws_ami.amazon_linux_ireland.id
  instance_type          = "t3.micro"
  vpc_security_group_ids = [aws_security_group.ireland_sg.id]
  tags                   = { Name = "Ireland-Server-10.2" }
}



Phase 2: Documentation Note
You should definitely add this error to your Troubleshooting & Lessons Learned table later.

Issue: Error: Reference to undeclared resource for aws_ami.
Root Cause: Terraform root modules are isolated by directory. Data sources defined in Scenario-10 were not available in Scenario-06.
Specialist Insight: AMIs are region-bound. When working with Global VPC Peering, you must define regional providers for your data sources to fetch the correct AMI ID for each location.


This error is another "Technical Specialist" classic! It’s a network mismatch.

The Root Cause
When you create a Security Group, it is tied to a specific VPC ID. In our code, we created us_sg inside us_vpc.

However, in your aws_instance blocks, we didn't specify a Subnet. When you don't specify a subnet, AWS tries to launch the instance into the Default VPC. Since the Security Group belongs to our new VPC and the Instance is trying to launch in the Default VPC, AWS blocks the operation with the error: "belong to different networks."

The Fix: Add Subnets
To fix this, we need to create one subnet in each VPC and tell the instances to land there. Add these blocks to your main.tf:

Terraform
# 1. Create Subnet in US VPC
resource "aws_subnet" "us_subnet" {
  vpc_id            = aws_vpc.us_vpc.id
  cidr_block        = "10.1.1.0/24"
  availability_zone = "us-east-1a"
  tags              = { Name = "US-Subnet" }
}

# 2. Create Subnet in Ireland VPC
resource "aws_subnet" "ireland_subnet" {
  provider          = aws.ireland
  vpc_id            = aws_vpc.ireland_vpc.id
  cidr_block        = "10.2.1.0/24"
  availability_zone = "eu-west-1a"
  tags              = { Name = "Ireland-Subnet" }
}




Step 2: Update the Instance Blocks
Now, update your aws_instance resources to include the subnet_id.

Important: I am also adding associate_public_ip_address = true so you can connect via the browser, and ensuring the us_server has the correct subnet_id.

Terraform
# US Instance
resource "aws_instance" "us_server" {
  ami                         = data.aws_ami.amazon_linux_us.id
  instance_type               = "t3.micro"
  subnet_id                   = aws_subnet.us_subnet.id # <--- Connect to the right network
  vpc_security_group_ids      = [aws_security_group.us_sg.id]
  associate_public_ip_address = true 

  tags = { Name = "US-Server-10.1" }
}

# Ireland Instance
resource "aws_instance" "ireland_server" {
  provider                    = aws.ireland
  ami                         = data.aws_ami.amazon_linux_ireland.id
  instance_type               = "t3.micro"
  subnet_id                   = aws_subnet.ireland_subnet.id # <--- Connect to the right network
  vpc_security_group_ids      = [aws_security_group.ireland_sg.id]
  associate_public_ip_address = true

  tags = { Name = "Ireland-Server-10.2" }
}




The Final Test (The "Specialist" Proof)
To prove this works for your interview portfolio, you need to show one server talking to the other using Private IPs, not Public ones.

Find the Private IPs:

Go to your AWS Console.

Find the Ireland-Server-10.2 (in the eu-west-1 region). Write down its Private IP (should be 10.2.1.x).

Find the US-Server-10.1 (in the us-east-1 region).

Connect to the US Server:

Use EC2 Instance Connect from the console to log into the US-Server.

The Cross-Region Ping:

In the US server's terminal, run:

Bash
ping <Ireland_Private_IP>
What to look for in the results:
Success: You should see replies coming back. This proves the Peering, the Route Tables, and the Security Groups are all correctly configured.

Latency: Notice the time=XXms. Because the signal has to travel from Virginia to Ireland and back, you’ll likely see ~70ms to 90ms.

Interview Tip: Mentioning this latency shows you understand the physical reality of global infrastructure.



To make sure this test is valid for your documentation, you need to distinguish between how you get into the server and what you are testing.

1. Connecting TO the US Server (The Entry)
When you click "Connect" in the AWS Console for your US-Server-10.1:

Use the Public IP: The AWS Console needs the Public IP to establish the session from your browser to the instance.

Because we added associate_public_ip_address = true in your Terraform code, the console will handle this automatically. You don't need to manually type it in.

2. Testing FROM the US to Ireland (The Peering Test)
Once you are inside the black terminal window (the US server):

Use the Private IP of the Ireland Server: This is the most important part of the lab.

The goal of VPC Peering is to allow two private networks to talk without going over the public internet. If you ping the Public IP, you aren't testing the peering; you’re just testing the internet.

By pinging the Private IP (10.2.1.x), you are proving that the traffic is traveling through the AWS Peering Bridge we built.



The "Specialist" Command sequence:
Once that browser terminal opens for the US server, run this:

Bash
# Verify you are in the 10.1 (US) network
hostname -I

# Ping the 10.2 (Ireland) network
ping <Ireland_Private_IP>



The "Specialist" Analysis of the Error
The warning says: "Instance is not in a public subnet."

Even though your instance has a Public IP (44.200.230.14), it is currently "stranded." In AWS, a subnet only becomes a "Public Subnet" when it has a Route Table entry that sends 0.0.0.0/0 traffic to an Internet Gateway (IGW). Since we only created the "Peering" route, the subnet doesn't know how to talk to the internet (which is how the AWS Console connects to it).

The Fix: Adding the "Internet Bridge"
We need to add three things to your main.tf for both regions: an Internet Gateway, a route to that gateway, and SSH access in the Security Group.

1. Create the Internet Gateways

# US Internet Gateway
resource "aws_internet_gateway" "us_igw" {
  vpc_id = aws_vpc.us_vpc.id
  tags   = { Name = "US-IGW" }
}

# Ireland Internet Gateway
resource "aws_internet_gateway" "ireland_igw" {
  provider = aws.ireland
  vpc_id   = aws_vpc.ireland_vpc.id
  tags     = { Name = "Ireland-IGW" }
}


2. Add the Default Routes (to the Internet)


# Route US Subnet to US IGW
resource "aws_route" "us_internet_route" {
  route_table_id         = aws_vpc.us_vpc.main_route_table_id
  destination_cidr_block = "0.0.0.0/0"
  gateway_id             = aws_internet_gateway.us_igw.id
}

# Route Ireland Subnet to Ireland IGW
resource "aws_route" "ireland_internet_route" {
  provider               = aws.ireland
  route_table_id         = aws_vpc.ireland_vpc.main_route_table_id
  destination_cidr_block = "0.0.0.0/0"
  gateway_id             = aws_internet_gateway.ireland_igw.id
}


3. Update Security Groups to allow SSH (Port 22)
Your current SG only allows PING. We need to add SSH so the console can connect. Update your aws_security_group resources:

# Add this 'ingress' block to BOTH us_sg and ireland_sg
ingress {
  from_port   = 22
  to_port     = 22
  protocol    = "tcp"
  cidr_blocks = ["0.0.0.0/0"] # Allow SSH
}


Action Plan:
Add the Internet Gateway and Route blocks to your main.tf.

Update the Security Groups to include Port 22.

Run terraform apply.

Refresh the AWS Console page. That yellow warning box should disappear, and the Connect button should work.



That is the perfect starting point! If it stays on that line without showing "64 bytes from...", it means the packet is being sent out, but the reply isn't coming back.

If it is hanging there, do not worry—this is the final "Phase 2" troubleshooting step.

What a Success looks like:
If the peering is 100% open, you should see this within a second:
64 bytes from 10.2.1.204: icmp_seq=1 ttl=235 time=72.4 ms

If it is NOT showing replies, check these 2 things:
The Ireland Security Group (Inbound): Go to the Ireland region in your console. Does the Security Group for the Ireland server allow All ICMP - IPv4 from the source 10.1.0.0/16? If it only allows its own local network, it will drop the packet from the US.

The Ireland Route Table:
Go to the Route Table for your Ireland VPC. Does it have a route that says:
10.1.0.0/16 -> pcx-xxxxxxxxx (The Peering Connection)?
Without this, the Ireland server gets your ping but doesn't know the way back to the US.

Specialist Tip: Check the Latency
When you do get the reply, look closely at the time=XXms.

Intra-region (same city): Usually < 1-2ms.

Cross-region (Global): Usually 70ms to 100ms.

In your interview, you can say: "I verified the peering by pinging the Ireland instance from Virginia. I observed a 75ms round-trip latency, which is expected for transatlantic traffic on the AWS backbone."




They confirm that your Ireland Server is sitting at 10.2.1.204 and everything looks healthy on the surface.

Since your ping is sitting there without a response, we are in the "Phase 2" troubleshooting stage. This is where you earn your "Technical Specialist" stripes. If a ping hangs, it’s almost always one of two things: The Gate (Security Group) or The Road (Route Table).

The "Specialist" Check-list (Ireland Side)
Since the US is sending the packet, we need to make sure Ireland knows how to receive it and send it back.

1. Check the "Gate" (Security Group)
Go to your Ireland EC2 Console, click on the Security tab for the Ireland server, and verify:

Inbound Rules: Does it have a rule for All ICMP - IPv4?

Source: Is the source set to 10.1.0.0/16 (the US VPC CIDR)?

If the source is 0.0.0.0/0 or just its own local IP, it will ignore the US ping.

2. Check the "Road" (Route Table)
This is the most common mistake. Go to VPC > Route Tables in the Ireland region:

Select the Route Table associated with subnet-0e7553276ff27b30b.

Click the Routes tab.

Is there a route for 10.1.0.0/16?

Target: It must point to the Peering Connection (pcx-xxxxxx).

When you added the Internet Gateway route (0.0.0.0/0), you might have overwritten or missed the peering route.




How to fix it in Terraform (The Permanent Fix)
If you find a missing route, don't just fix it in the console—update your code so your documentation stays accurate. Ensure these two blocks are still in your main.tf and haven't been commented out:

# This tells Ireland how to find the US
resource "aws_route" "ireland_to_us_peering" {
  provider                  = aws.ireland
  route_table_id            = aws_vpc.ireland_vpc.main_route_table_id
  destination_cidr_block    = "10.1.0.0/16" 
  vpc_peering_connection_id = aws_vpc_peering_connection.us_to_ireland.id
}

# This tells the US how to find Ireland
resource "aws_route" "us_to_ireland_peering" {
  route_table_id            = aws_vpc.us_vpc.main_route_table_id
  destination_cidr_block    = "10.2.0.0/16"
  vpc_peering_connection_id = aws_vpc_peering_connection.us_to_ireland.id
}



The "Debug" Command
Try running this command on your US Server terminal. It will show you exactly where the packet stops:

Bash
traceroute 10.2.1.204
(If traceroute isn't installed, run sudo yum install traceroute -y first).

If it stops at the first hop: The US doesn't know where to send the packet (Route Table issue in US).

If it gets to the AWS backbone but dies at the end: Ireland is receiving it but dropping it (Security Group issue in Ireland).

Check those two things in the Ireland Console and let me know which one was the "culprit" for your Troubleshooting Log!



There is the "culprit" for your Troubleshooting Log! You’ve found the exact reason the ping is hanging.The Technical Insight: TCP vs. ICMPIn networking, Ping does not use TCP. It uses a different protocol called ICMP (Internet Control Message Protocol).TCP: Used for things like SSH (Port 22) or Web traffic (Port 80/443). It requires a "handshake" to connect.ICMP: Used for "echo requests" (Pings). It doesn't have port numbers like 22 or 80.If your Security Group only allows TCP, it is like having a door that only opens for people with a specific key. The ICMP packet arrives, sees that the door only opens for TCP, and gets dropped immediately.How to Fix the "Gate" (Ireland Side)To get the successful ping for your documentation, you need to add an ICMP rule to the Security Group in Ireland.Option A: The Terraform Fix (Recommended)Update your ireland_sg resource in your main.tf to look like this:




resource "aws_security_group" "ireland_sg" {
  provider = aws.ireland
  vpc_id   = aws_vpc.ireland_vpc.id

  # Rule 1: Allow SSH (TCP) - So you can log in
  ingress {
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  # Rule 2: Allow PING (ICMP) - So the US can reach it
  ingress {
    from_port   = -1            # -1 means "All" for ICMP
    to_port     = -1            # -1 means "All" for ICMP
    protocol    = "icmp"        # <--- This is the missing piece!
    cidr_blocks = ["10.1.0.0/16"] # Only allow from your US VPC
  }

  egress {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }
}



Option B: The Console Fix (Quick Test)
Go to the Ireland Security Group in the AWS Console.

Click Edit inbound rules.

Add a new rule:

Type: All ICMP - IPv4

Source: Custom -> 10.1.0.0/16

Save the rule and go back to your US terminal. The ping should start working instantly.



That is a beautiful sight! 65.7 ms of latency across the Atlantic Ocean. You just performed a transatlantic "digital high-five" using private networking.

This proves that your packets are traveling from N. Virginia to Dublin entirely on the AWS private backbone, bypassing the public internet. As a Technical Specialist, this is exactly the kind of evidence you want to show to prove you can build secure, global infrastructure.
